MT Net signed update artifacts

This repository contains only the signed release package and update manifest served to installed MT Net clients. It contains no application source code. Update integrity is enforced by the Ed25519 signature in every manifest, verified against the public keys embedded in the client.
